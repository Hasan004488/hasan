
<% options.forEach(option => { %>
    <option value="<%= option.property %>" 
        <%= query?.identifier === option.property ? "selected" : "" %>
    >
        <%= option.name %>
    </option>
<% }) %>